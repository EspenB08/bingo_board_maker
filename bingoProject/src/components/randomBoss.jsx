import React from "react";
import runescapeBosses from "../assets/BossList";

function randomBoss(){
  const [RandomizedBoss, setRandomizedBoss] = useState(null);

  const handleRandomizedBoss = (event) => {
    setSelectedBoss(event.target.value);
}
return(
    <div>
        
    </div>
)
}
export default randomBoss